---
alwaysApply: true
---


- 代码注释一律使用英文。

# 左花括号位置
- namespace、class/struct/union/enum、function等的左花括号另起一行，其余则不必；

# 代码符号命名规则
- 使用驼峰命名规则。
- 全局非静态数据以g开头，全局静态数据以s开头；
- 成员数据以m开头；
- enum值全大写；
- 函数名、数据类型名一律大写字母开头；
